# Detecting sensor prescence 
- Only reply to RTR if the sensor is plugged in 
- Add in operational mode checking - see if library supports => `DEVICE_STATUS` register apparently 
- Need to burn-in for 24-48 hours 